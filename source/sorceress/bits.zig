const std = @import("std");


// Returns a 1-based value, or 0 if no bits are set.
pub fn ffs(data: *const u8, n: usize) u64 {
    _ = data;
    _ = n;
}

pub fn popcnt(data: *const u8, n: usize) u64 {
    _ = data;
    _ = n;
}

